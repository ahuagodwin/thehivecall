# Base URL for the backend API. If unset, requests default to /api and the
# contact/newsletter services gracefully fall back to a simulated response
# so the UI works fully out of the box without a backend.
VITE_API_BASE_URL=/api
